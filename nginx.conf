worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    keepalive_timeout  65;
    server_tokens off;

    upstream app_servers {
        server app:9000;
    }

    server {
        listen 80;
        server_name _;  # Catch-all for all domains

        root /var/www/html/public;  # Single html installation
        index index.php index.html;
        client_max_body_size 100M;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            include fastcgi_params;
            fastcgi_pass app_servers;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME /var/www/html/public$fastcgi_script_name;
        }

        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|otf|mp4|webm|ogg|mp3|wav|json|txt)$ {
            expires max;
            log_not_found off;
            access_log off;
        }

        error_page 404 /index.php;
    }
}
